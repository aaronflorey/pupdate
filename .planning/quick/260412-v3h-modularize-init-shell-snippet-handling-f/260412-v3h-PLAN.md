---
phase: quick-260412-v3h-modularize-init-shell-snippet-handling-f
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/pupdate/init.go
  - cmd/pupdate/init_snippets.go
  - cmd/pupdate/init_test.go
autonomous: true
requirements:
  - V3H-MOD-01
must_haves:
  truths:
    - "`pupdate init` still emits the correct shell snippet for bash, zsh, and fish."
    - "Shell snippet selection logic is isolated from command wiring so future shell additions do not require editing multiple branches."
    - "Unsupported shell handling remains actionable and consistent with supported shell list."
  artifacts:
    - path: "cmd/pupdate/init_snippets.go"
      provides: "centralized shell snippet constants and snippet lookup helpers"
      contains: "bash/zsh/fish snippet definitions and selector"
    - path: "cmd/pupdate/init.go"
      provides: "thin init command wiring that delegates snippet resolution"
      contains: "newInitCmd + resolveShell using shared helper"
    - path: "cmd/pupdate/init_test.go"
      provides: "regression coverage for snippet output and unsupported shell behavior"
      contains: "TestInit*"
  key_links:
    - from: "cmd/pupdate/init.go"
      to: "cmd/pupdate/init_snippets.go"
      via: "init command calls snippet selector using resolved shell"
      pattern: "snippet.*(resolved|shell)"
    - from: "cmd/pupdate/init_test.go"
      to: "cmd/pupdate/init.go"
      via: "command execution contract tests"
      pattern: "newRootCmd\(\).*init"
---

<objective>
Modularize init shell snippet handling so snippet data and lookup logic are separated from command plumbing.

Purpose: Improve readability/maintainability of `init` without changing user-visible behavior or shell hook performance.
Output: Extracted snippet module + slimmer `init` command + regression tests proving behavior parity.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@cmd/pupdate/init.go
@cmd/pupdate/init_test.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock current init shell-output behavior with focused regression tests</name>
  <files>cmd/pupdate/init_test.go</files>
  <behavior>
    - Test 1: `init --shell bash|zsh|fish` emits shell-specific hook markers and `pupdate run --quiet`.
    - Test 2: unsupported shell returns actionable error listing supported shells.
    - Test 3: default shell resolution still falls back predictably for empty/unknown shell env.
  </behavior>
  <action>Add/adjust tests to define parity before refactor; keep assertions tied to observable command output (not private implementation). Preserve stderr-visibility assertions (no `2>/dev/null`) and avoid broad unrelated test changes.</action>
  <verify>
    <automated>go test ./cmd/pupdate -run TestInit -count=1</automated>
  </verify>
  <done>Init behavior is fully captured by tests that fail if snippet selection or shell support contracts regress during refactor.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract shell snippet catalog and selector into dedicated module</name>
  <files>cmd/pupdate/init.go, cmd/pupdate/init_snippets.go</files>
  <behavior>
    - `newInitCmd` delegates snippet retrieval to helper(s) instead of inline switch/constant sprawl.
    - Snippet definitions and supported-shell mapping live in one place.
    - `resolveShell` and unsupported-shell errors remain consistent with supported list.
  </behavior>
  <action>Create `cmd/pupdate/init_snippets.go` for snippet constants and lookup helper(s), then simplify `init.go` to orchestration-only logic. Keep binary/runtime behavior unchanged (no extra subprocess/file scans) and retain explicit support for bash, zsh, and fish.</action>
  <verify>
    <automated>go test ./cmd/pupdate -run TestInit -count=1</automated>
  </verify>
  <done>Snippet handling is modularized into a dedicated file, `init.go` is shorter and easier to read, and all init regression tests pass unchanged in behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI output → user shell startup files | Generated snippet text is executed in an interactive shell context. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-v3h-01 | T | `cmd/pupdate/init_snippets.go` snippet templates | mitigate | Keep snippets as static constants with no user-input interpolation during refactor. |
| T-quick-v3h-02 | D | `cmd/pupdate/init.go` init hook behavior | mitigate | Preserve current lightweight hook contract (`pupdate run --quiet` only) and avoid adding additional runtime work. |
| T-quick-v3h-03 | R | unsupported shell error surface | mitigate | Continue returning explicit supported-shell message and cover it in init tests. |
</threat_model>

<verification>
- `go test ./cmd/pupdate -run TestInit -count=1`
- `go test ./cmd/pupdate -count=1`
</verification>

<success_criteria>
- Init snippet constants/selection logic are moved to a dedicated module with no behavior drift.
- `newInitCmd` remains thin and readable, delegating snippet handling instead of embedding all snippet details inline.
- Existing shell contracts (bash/zsh/fish support, actionable errors, quiet-run snippet behavior) remain green under tests.
</success_criteria>

<output>
After completion, create `.planning/quick/260412-v3h-modularize-init-shell-snippet-handling-f/260412-v3h-SUMMARY.md`
</output>
