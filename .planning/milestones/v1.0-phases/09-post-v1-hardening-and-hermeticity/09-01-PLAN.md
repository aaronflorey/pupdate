---
phase: 09-post-v1-hardening-and-hermeticity
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: []
---

# Plan 09-01 - Fix Cargo.lock case handling

## Objective

Make Rust lockfile detection and freshness behavior consistent around the canonical `Cargo.lock` filename so Linux and macOS runs behave predictably.

## Scope

- Audit the Rust detector contract around `Cargo.lock` versus lowercase fixture coverage.
- Update detection logic or tests so the case-handling rule is explicit and stable.
- Preserve existing manager selection and matched-file state keys unless a smaller-safe adjustment is required.

## Tasks

1. Confirm the intended canonical filename behavior in `internal/detection` and related freshness/state call sites.
2. Implement the minimal detection/test change needed to make case handling deterministic.
3. Add or update regression coverage for the chosen contract on case-sensitive filesystems.

## Verification

- `go test ./internal/detection -count=1`
- `go test ./internal/freshness -count=1`

## Done When

- Rust detection no longer has ambiguous `Cargo.lock` case behavior.
- Regression tests codify the supported filename contract.
