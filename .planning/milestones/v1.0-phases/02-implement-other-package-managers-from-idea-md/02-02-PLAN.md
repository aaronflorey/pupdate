---
phase: 02-implement-other-package-managers-from-idea-md
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - cmd/pupdate/run.go
  - cmd/pupdate/run_test.go
autonomous: true
requirements: [ECO-02, ECO-03, ECO-04]
must_haves:
  truths:
    - "When new lockfiles are detected, pupdate selects the matching manager command with safe defaults."
    - "Unsupported or missing binaries are skipped with explicit stderr reasons, not hard failures."
    - "Existing bun/composer safety defaults continue to work unchanged."
  artifacts:
    - path: "cmd/pupdate/run.go"
      provides: "Manager-plan selection for Node, Python, Go, and Rust ecosystems"
      exports: ["newRunCmd", "selectManagerPlan"]
    - path: "cmd/pupdate/run_test.go"
      provides: "Exact argument and behavior assertions for all supported managers"
      min_lines: 500
  key_links:
    - from: "internal/detection/detector.go"
      to: "cmd/pupdate/run.go"
      via: "DetectionResult.Managers consumed by selectManagerPlan"
      pattern: 'result\.Managers|selectManagerPlan'
    - from: "cmd/pupdate/run.go"
      to: "exec.LookPath"
      via: "runtime PATH manager discovery"
      pattern: 'lookPath\(plan\.Manager\)'
---

<objective>
Implement safe execution plans for newly detected ecosystems/managers while preserving low-risk behavior and PATH-based runtime resolution.

Purpose: Convert expanded detection results into concrete, safe install/update commands for ECO-02/ECO-03/ECO-04.
Output: Updated manager-plan logic and tests covering args, ambiguity handling, and skip reasons.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-implement-other-package-managers-from-idea-md/02-RESEARCH.md
@.planning/phases/02-implement-other-package-managers-from-idea-md/02-01-SUMMARY.md
@cmd/pupdate/run.go
@cmd/pupdate/run_test.go

<interfaces>
From cmd/pupdate/run.go:
```go
type managerPlan struct {
    Manager string
    Args    []string
}

func selectManagerPlan(result detection.DetectionResult) (managerPlan, bool, string)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement manager plans for npm/pnpm/yarn + Python/Go/Rust per ECO-02/ECO-03/ECO-04</name>
  <files>cmd/pupdate/run.go, cmd/pupdate/run_test.go</files>
  <read_first>cmd/pupdate/run.go, cmd/pupdate/run_test.go, internal/detection/model.go, .planning/phases/02-implement-other-package-managers-from-idea-md/02-RESEARCH.md</read_first>
  <behavior>
    - Test 1: Node lockfile-manager combinations map to exact safe commands for bun/npm/pnpm/yarn.
    - Test 2: Python detection maps to uv/poetry/pip command plans using non-interactive safe flags.
    - Test 3: Go and Rust map to `go mod download` and `cargo fetch --locked`.
    - Test 4: Ambiguous multi-manager node detections still skip with explicit reason.
  </behavior>
  <action>Extend `selectManagerPlan` to support managers from phase 02 detection outputs: Node managers `bun`, `npm`, `pnpm`, `yarn`; Python managers `uv`, `poetry`, `pip`; Go and Rust ecosystems with fixed managers. Use these exact args per research/safety constraints: bun `install --frozen-lockfile --ignore-scripts`; npm `ci --ignore-scripts`; pnpm `install --frozen-lockfile --ignore-scripts`; yarn `install --frozen-lockfile --ignore-scripts`; uv `sync --frozen`; poetry `install --no-interaction --sync`; pip `install -r requirements.txt --disable-pip-version-check --no-input`; go `mod download`; cargo `fetch --locked`. Preserve PATH runtime checks via existing `lookPath` branch; do not introduce hardcoded binary paths.</action>
  <acceptance_criteria>
    - `cmd/pupdate/run.go` contains each manager command literal listed in action
    - `cmd/pupdate/run_test.go` contains arg-equality assertions for npm, pnpm, yarn, uv, poetry, pip, go, cargo
    - `go test ./cmd/pupdate -run 'TestSelectManagerPlan|TestRunPrintsErrorStatusWhenInstallFails' -count=1` exits 0
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd/pupdate -run 'TestSelectManagerPlan|TestRunPrintsErrorStatusWhenInstallFails' -count=1</automated>
  </verify>
  <done>Run command can build safe manager plans for all non-git phase-2 ecosystems with green tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add run-path behavior tests for skip/run messaging on newly supported managers</name>
  <files>cmd/pupdate/run_test.go</files>
  <read_first>cmd/pupdate/run_test.go, cmd/pupdate/run.go</read_first>
  <behavior>
    - Test 1: Missing manager binary yields `pupdate: skip <ecosystem> (<manager> not found on PATH)` for at least one new manager.
    - Test 2: Successful plan selection emits `pupdate: run <manager> <args>` line for newly supported ecosystems.
  </behavior>
  <action>Add focused run command tests using existing stubbing patterns (`lookPath`, `execCommand`, temp lockfiles) to verify stderr status contract remains stable for new managers. Cover one Node manager beyond bun (npm/pnpm/yarn), one Python manager, and one of Go/Rust. Keep tests deterministic and avoid integration with actual package manager binaries.</action>
  <acceptance_criteria>
    - `cmd/pupdate/run_test.go` includes assertions matching `pupdate: skip .* not found on PATH` and `pupdate: run ` for new managers
    - `go test ./cmd/pupdate -run 'TestRun.*(skip|run|PATH|path|manager)' -count=1` exits 0
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd/pupdate -run 'TestRun.*(skip|run|PATH|path|manager)' -count=1</automated>
  </verify>
  <done>Expanded manager support preserves transparent status behavior and remains fully test-covered.</done>
</task>

</tasks>

<verification>
- `go test ./cmd/pupdate -count=1`
- `go test ./... -count=1`
</verification>

<success_criteria>
- ECO-02/ECO-03/ECO-04 manager execution paths are implemented with explicit safe arguments.
- Runtime PATH discovery and status-output contracts remain intact across new managers.
</success_criteria>

<output>
After completion, create `.planning/phases/02-implement-other-package-managers-from-idea-md/02-02-SUMMARY.md`
</output>
