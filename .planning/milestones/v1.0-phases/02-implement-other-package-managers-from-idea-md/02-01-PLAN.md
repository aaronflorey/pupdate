---
phase: 02-implement-other-package-managers-from-idea-md
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/detection/matrix.go
  - internal/detection/detector.go
  - internal/detection/detector_test.go
  - internal/detection/model.go
  - cmd/pupdate/run_test.go
autonomous: true
requirements: [ECO-02, ECO-03, ECO-04]
must_haves:
  truths:
    - "Node projects using npm/pnpm/yarn lockfiles are detected with explicit manager identity."
    - "Python, Go, and Rust projects are detected from their canonical lockfile/manifest signals."
    - "Detection output remains deterministic and test-locked for multi-ecosystem repos."
  artifacts:
    - path: "internal/detection/matrix.go"
      provides: "Expanded signal->ecosystem and node lockfile->manager mapping"
      contains: "package-lock.json, pnpm-lock.yaml, yarn.lock, uv.lock, poetry.lock, requirements.txt, go.mod, cargo.lock"
    - path: "internal/detection/detector_test.go"
      provides: "Detection coverage for all newly supported lockfiles"
      min_lines: 220
    - path: "cmd/pupdate/run_test.go"
      provides: "JSON output assertions for expanded manager detection"
      min_lines: 420
  key_links:
    - from: "internal/detection/matrix.go"
      to: "internal/detection/detector.go"
      via: "signalToEcosystem + nodeManagerByLockfile lookups"
      pattern: 'signalToEcosystem\[signal\]|nodeManagerByLockfile\[name\]'
    - from: "internal/detection/detector.go"
      to: "cmd/pupdate/run_test.go"
      via: "DetectionResult.Managers and MatchedFiles in run payload"
      pattern: "Managers:|MatchedFiles:"
---

<objective>
Expand detection contracts so Phase 2 can execute npm/pnpm/yarn, Python, Go, and Rust update flows without ambiguity.

Purpose: Build a deterministic, test-locked detection surface before adding more execution logic.
Output: Updated detection matrix/model/tests and run payload expectations for new ecosystems/managers.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-implement-other-package-managers-from-idea-md/02-RESEARCH.md
@internal/detection/model.go
@internal/detection/matrix.go
@internal/detection/detector.go

<interfaces>
From internal/detection/model.go:
```go
type DetectionResult struct {
    Ecosystem    Ecosystem
    Managers     []string
    MatchedFiles []string
    Warnings     []Warning
}
```

From internal/detection/matrix.go:
```go
var ecosystemSignals map[Ecosystem][]string
var nodeManagerByLockfile map[string]string
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend detection matrix and manager mapping for ECO-02/ECO-03/ECO-04</name>
  <files>internal/detection/matrix.go, internal/detection/model.go, internal/detection/detector.go, internal/detection/detector_test.go</files>
  <read_first>internal/detection/model.go, internal/detection/matrix.go, internal/detection/detector.go, internal/detection/detector_test.go, .planning/phases/02-implement-other-package-managers-from-idea-md/02-RESEARCH.md</read_first>
  <behavior>
    - Test 1: Detect identifies npm/pnpm/yarn lockfiles and sets node manager list deterministically.
    - Test 2: Detect identifies Python (`uv.lock`, `poetry.lock`, `requirements.txt`), Go (`go.mod`), and Rust (`cargo.lock`) signals.
    - Test 3: Detect remains case-insensitive and ignores unrelated backup/swap files.
  </behavior>
  <action>Add Node signals (`package-lock.json`, `pnpm-lock.yaml`, `yarn.lock`) and map them to managers (`npm`, `pnpm`, `yarn`) in `nodeManagerByLockfile` per ECO-02. Add Python/Go/Rust signals in `ecosystemSignals` per ECO-03/ECO-04 (`uv.lock`, `poetry.lock`, `requirements.txt`, `go.mod`, `cargo.lock`). Keep existing lowercasing canonicalization in detector flow; do not add recursive scans or directory walking beyond `os.ReadDir` to preserve latency constraints from AGENTS.md. Add/adjust detection tests so multi-lockfile node repos include all matched lockfiles and manager list is deterministic.</action>
  <acceptance_criteria>
    - `internal/detection/matrix.go` contains each new signal literal: `package-lock.json`, `pnpm-lock.yaml`, `yarn.lock`, `uv.lock`, `poetry.lock`, `requirements.txt`, `go.mod`, `cargo.lock`
    - `internal/detection/matrix.go` contains node manager mapping entries for `npm`, `pnpm`, `yarn`
    - `go test ./internal/detection -count=1` exits 0
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/detection -count=1</automated>
  </verify>
  <done>Detector returns expanded ecosystems/managers with deterministic ordering and passing tests for ECO-02/ECO-03/ECO-04.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock run JSON detection payload expectations for expanded matrix</name>
  <files>cmd/pupdate/run_test.go</files>
  <read_first>cmd/pupdate/run_test.go, cmd/pupdate/run.go, internal/detection/detector.go</read_first>
  <behavior>
    - Test 1: `pupdate run` JSON includes newly detected ecosystems and manager names for representative lockfiles.
    - Test 2: Node manager list in payload reflects detected lockfile(s) without dropping bun behavior.
  </behavior>
  <action>Update/add run payload tests so JSON output assertions include npm/pnpm/yarn and Python/Go/Rust detection coverage produced by Task 1 contracts. Preserve existing stdout JSON shape (`directory`, `ecosystems`, `warnings`) and keep status-output checks on stderr unchanged. Do not modify execution/install behavior in this task.</action>
  <acceptance_criteria>
    - `cmd/pupdate/run_test.go` contains assertions referencing at least one new manager among `npm`, `pnpm`, `yarn`
    - `cmd/pupdate/run_test.go` contains assertions for at least one non-node ecosystem from `python`, `go`, or `rust`
    - `go test ./cmd/pupdate -run 'TestRunOutput|TestRunOutputIncludesNodeManagers' -count=1` exits 0
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd/pupdate -run 'TestRunOutput|TestRunOutputIncludesNodeManagers' -count=1</automated>
  </verify>
  <done>Run payload tests encode expanded detection contracts and remain green without changing command execution semantics.</done>
</task>

</tasks>

<verification>
- `go test ./internal/detection ./cmd/pupdate -count=1`
- `go test ./... -count=1`
</verification>

<success_criteria>
- ECO-02/ECO-03/ECO-04 detection signals are represented in code and validated by tests.
- Detection output remains deterministic and consumable by run command tests.
</success_criteria>

<output>
After completion, create `.planning/phases/02-implement-other-package-managers-from-idea-md/02-01-SUMMARY.md`
</output>
