---
phase: 01-mvp-auto-update-cli
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/detection/matrix.go
  - internal/detection/detector.go
  - internal/detection/detector_test.go
  - cmd/pupdate/run.go
  - cmd/pupdate/run_test.go
autonomous: true
requirements:
  - DET-01
  - DET-02
  - EXEC-01
  - EXEC-02
  - EXEC-03
  - ECO-01
must_haves:
  truths:
    - "`pupdate run` detects bun/composer repos and selects the correct manager per lockfile."
    - "Install commands run in safe/frozen mode and skip lifecycle scripts by default."
    - "Manager executable is resolved from current PATH at runtime, not hardcoded."
  artifacts:
    - path: "internal/detection/matrix.go"
      provides: "MVP detection signal matrix for bun/composer"
      contains: "bun.lock and composer.lock mapping"
    - path: "cmd/pupdate/run.go"
      provides: "Manager plan selection and install command arguments"
      exports: ["newRunCmd", "selectManagerPlan"]
    - path: "cmd/pupdate/run_test.go"
      provides: "CLI behavior tests for manager selection and safety flags"
  key_links:
    - from: "internal/detection/detector.go"
      to: "cmd/pupdate/run.go"
      via: "DetectionResult.Ecosystem and Managers"
      pattern: "Detect\(\) -> selectManagerPlan"
    - from: "cmd/pupdate/run.go"
      to: "runtime PATH"
      via: "lookPath(plan.Manager)"
      pattern: "lookPath\(plan.Manager\)"
---

<objective>
Lock MVP detection and install execution to bun/composer with safe defaults and PATH-driven manager resolution.

Purpose: Deliver the minimum reliable update engine for phase-1 ecosystems.
Output: Deterministic detection + safe install command planning + passing tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-mvp-auto-update-cli/01-RESEARCH.md
@internal/detection/matrix.go
@internal/detection/detector.go
@cmd/pupdate/run.go

<interfaces>
From internal/detection/model.go:
```go
type DetectionResult struct {
    Ecosystem   Ecosystem
    Managers    []string
    MatchedFiles []string
    Warnings    []Warning
}
```

From cmd/pupdate/run.go:
```go
func selectManagerPlan(result detection.DetectionResult) (managerPlan, bool, string)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Restrict MVP detection signals to bun/composer only</name>
  <files>internal/detection/matrix.go, internal/detection/detector_test.go</files>
  <read_first>internal/detection/matrix.go, internal/detection/detector.go, internal/detection/detector_test.go</read_first>
  <behavior>
    - Test 1: directory with `bun.lock` returns node ecosystem with manager `bun`.
    - Test 2: directory with `composer.lock` returns php ecosystem.
    - Test 3: directory with `package-lock.json` only returns no ecosystem (MVP excludes npm lockfiles for now).
  </behavior>
  <action>Update `ecosystemSignals` and `nodeManagerByLockfile` to support only `bun.lock` for node and `composer.lock` for php (per ECO-01, DET-01). Remove non-MVP lockfile entries from matrix and update tests to assert those files are ignored for this phase. Keep detector behavior deterministic and case-insensitive for canonical names.</action>
  <acceptance_criteria>
    - `internal/detection/matrix.go` contains `bun.lock` and `composer.lock` mappings.
    - `internal/detection/matrix.go` does not contain `package-lock.json`, `yarn.lock`, or `pnpm-lock` strings.
    - `go test ./internal/detection -count=1` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/detection -count=1</automated>
  </verify>
  <done>MVP detection covers only bun/composer with passing detector tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce safe install command plans with scripts disabled by default</name>
  <files>cmd/pupdate/run.go, cmd/pupdate/run_test.go</files>
  <read_first>cmd/pupdate/run.go, cmd/pupdate/run_test.go, .planning/ROADMAP.md</read_first>
  <behavior>
    - Test 1: bun manager plan includes frozen lockfile and ignore-scripts flags.
    - Test 2: composer manager plan includes no-interaction and no-scripts flags.
    - Test 3: unsupported manager/ecosystem still skips with explicit reason.
  </behavior>
  <action>Update `selectManagerPlan` to emit concrete safe defaults: bun → `install --frozen-lockfile --ignore-scripts`; composer → `install --no-interaction --prefer-dist --no-scripts` (per EXEC-01/02/03). Keep runtime PATH check via `lookPath(plan.Manager)` (per DET-02). Add/adjust command-layer tests so expected args are asserted exactly.</action>
  <acceptance_criteria>
    - `cmd/pupdate/run.go` contains `--frozen-lockfile` and `--ignore-scripts` in bun plan.
    - `cmd/pupdate/run.go` contains `--no-interaction`, `--prefer-dist`, and `--no-scripts` in composer plan.
    - `cmd/pupdate/run.go` still contains `lookPath(plan.Manager)`.
    - `go test ./cmd/pupdate -count=1` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd/pupdate -count=1</automated>
  </verify>
  <done>Install execution is safe-by-default and manager resolution remains PATH-based.</done>
</task>

</tasks>

<verification>
Run: `go test ./internal/detection ./cmd/pupdate -count=1`
</verification>

<success_criteria>
- `pupdate run` only plans installs for bun/composer ecosystems in MVP.
- Manager plan arguments enforce frozen/no-scripts defaults.
- Detection and run package tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/01-mvp-auto-update-cli/01-01-SUMMARY.md`
</output>
