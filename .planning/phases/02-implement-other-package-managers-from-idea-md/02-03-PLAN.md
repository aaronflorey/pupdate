---
phase: 02-implement-other-package-managers-from-idea-md
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - internal/freshness/engine.go
  - internal/freshness/engine_test.go
  - cmd/pupdate/run.go
  - cmd/pupdate/run_test.go
  - README.md
autonomous: true
requirements: [ECO-05]
must_haves:
  truths:
    - "Repos with stale git submodules trigger update behavior even when `.gitmodules` hash is unchanged."
    - "Repos with clean git submodules skip unnecessary submodule updates with clear status behavior."
    - "Users can see/document how git submodule support behaves in phase-2 README guidance."
  artifacts:
    - path: "internal/freshness/engine.go"
      provides: "Freshness decision path that can force update for git submodule drift"
      exports: ["Evaluate"]
    - path: "cmd/pupdate/run.go"
      provides: "Git manager plan execution and status output wiring"
      contains: "git submodule update --init --recursive"
    - path: "README.md"
      provides: "Documented ecosystem coverage + git submodule behavior"
      contains: "git submodule update --init --recursive"
  key_links:
    - from: "internal/freshness/engine.go"
      to: "cmd/pupdate/run.go"
      via: "DecisionUpdate for git ecosystem feeds install execution loop"
      pattern: "DecisionUpdate|selectManagerPlan"
    - from: "cmd/pupdate/run.go"
      to: "git submodule status --recursive"
      via: "drift detection check before deciding skip"
      pattern: "git.*submodule.*status"
---

<objective>
Add git submodule drift-aware behavior so pupdate can keep submodules synchronized (ECO-05) without regressing skip performance.

Purpose: Close the remaining phase requirement with deterministic, test-covered freshness + execution wiring.
Output: Freshness/runtime logic and tests for git submodule drift, plus README behavior updates.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-implement-other-package-managers-from-idea-md/02-RESEARCH.md
@.planning/phases/02-implement-other-package-managers-from-idea-md/02-02-SUMMARY.md
@internal/freshness/engine.go
@internal/freshness/engine_test.go
@cmd/pupdate/run.go
@cmd/pupdate/run_test.go
@README.md

<interfaces>
From internal/freshness/engine.go:
```go
type EcosystemDecision struct {
    Ecosystem string
    Decision  Decision
    Reason    string
    Lockfiles map[string]string
}

func Evaluate(dir string, detections []detection.DetectionResult, current state.FileState) ([]EcosystemDecision, error)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement git submodule drift detection override for freshness decisions (ECO-05)</name>
  <files>internal/freshness/engine.go, internal/freshness/engine_test.go, cmd/pupdate/run.go, cmd/pupdate/run_test.go</files>
  <read_first>internal/freshness/engine.go, internal/freshness/engine_test.go, cmd/pupdate/run.go, cmd/pupdate/run_test.go, .planning/phases/02-implement-other-package-managers-from-idea-md/02-RESEARCH.md</read_first>
  <behavior>
    - Test 1: If `.gitmodules` hash unchanged but `git submodule status --recursive` reports drift (`-`, `+`, or `U` prefix), decision is update.
    - Test 2: If `.gitmodules` unchanged and submodule status clean, decision remains skip.
    - Test 3: Run path executes `git submodule update --init --recursive` when update decision exists.
  </behavior>
  <action>Add git-specific drift-check logic per ECO-05: when ecosystem is git (from `.gitmodules` detection), run `git submodule status --recursive` and parse each line prefix. Treat prefixes `-`, `+`, or `U` as stale and force `DecisionUpdate` with reason `git submodule state drifted from recorded revision`; otherwise keep existing hash-based decision reason. Add `selectManagerPlan` handling for git ecosystem with manager `git` and args `submodule update --init --recursive`. Keep behavior non-blocking: on status command execution failure, print `pupdate: error git submodule status failed: <err>` and continue loop without panic.</action>
  <acceptance_criteria>
    - `internal/freshness/engine.go` contains parsing for submodule status prefixes `-`, `+`, and `U`
    - `cmd/pupdate/run.go` contains `git submodule update --init --recursive`
    - `go test ./internal/freshness ./cmd/pupdate -count=1` exits 0
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/freshness ./cmd/pupdate -count=1</automated>
  </verify>
  <done>Git submodule drift can trigger updates even without `.gitmodules` hash changes, with passing tests.</done>
</task>

<task type="auto">
  <name>Task 2: Update README ecosystem coverage and git submodule behavior contract</name>
  <files>README.md</files>
  <read_first>README.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, cmd/pupdate/run.go</read_first>
  <action>Add a concise section documenting phase-2 ecosystem support (npm/pnpm/yarn, uv/poetry/pip, go mod, cargo, git submodules) and list the exact git update command `git submodule update --init --recursive`. Include note that submodule drift detection uses `git submodule status --recursive` and can trigger updates even if `.gitmodules` content is unchanged. Preserve existing README structure and shell hook instructions.</action>
  <acceptance_criteria>
    - `README.md` contains ecosystem names `npm`, `pnpm`, `yarn`, `uv`, `poetry`, `pip`, `go`, `cargo`, `git submodule`
    - `README.md` contains literal command `git submodule update --init --recursive`
    - `README.md` retains existing `Shell Hook Setup (bash/zsh)` section
  </acceptance_criteria>
  <verify>
    <automated>go test ./... -count=1</automated>
  </verify>
  <done>README clearly reflects phase-2 behavior so users can predict submodule update decisions.</done>
</task>

</tasks>

<verification>
- `go test ./internal/freshness ./cmd/pupdate -count=1`
- `go test ./... -count=1`
</verification>

<success_criteria>
- ECO-05 is implemented with automated drift detection and update execution wiring.
- Documentation reflects full phase-2 ecosystem behavior and git submodule handling.
</success_criteria>

<output>
After completion, create `.planning/phases/02-implement-other-package-managers-from-idea-md/02-03-SUMMARY.md`
</output>
