---
phase: 04-restore-hook-visibility-and-script-opt-in-controls
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [EXEC-03, STAT-01, MILE-01]
---

# Plan 04-01 - Restore hook visibility and add script opt-in

## Objective

Restore visible hook-driven status output without regressing quiet-mode prompt behavior, and add an explicit lifecycle-script opt-in for supported package managers.

## Scope

- Remove shell-level stderr suppression from generated bash/zsh hook snippets.
- Add a `run --allow-scripts` flag that relaxes default script-blocking flags only where supported.
- Add regression coverage for quiet hook status visibility and script opt-in behavior.
- Update README behavior docs to reflect the restored hook output contract.

## Tasks

1. Update `init` snippets so hook-driven runs preserve stderr status lines.
2. Add `run --allow-scripts` and thread it into manager plan selection.
3. Add command tests for quiet stderr visibility and opt-in plan/exec behavior.
4. Refresh docs to describe visible hook status and the script opt-in flag.

## Verification

- `go test ./cmd/pupdate -count=1`
- `go test ./... -count=1`

## Done When

- Hook-generated snippets no longer discard stderr status lines.
- Lifecycle scripts remain disabled by default but can be enabled explicitly.
- Automated tests guard both contracts.
