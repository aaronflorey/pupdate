---
phase: 01-mvp-auto-update-cli
plan: 03
type: execute
wave: 2
depends_on:
  - 01
  - 02
files_modified:
  - cmd/pupdate/init.go
  - cmd/pupdate/run.go
  - cmd/pupdate/run_test.go
  - cmd/pupdate/init_test.go
  - README.md
autonomous: false
requirements:
  - SHELL-01
  - SHELL-02
  - STAT-01
must_haves:
  truths:
    - "User can run `pupdate init` and get usable bash/zsh hook snippets."
    - "Installed hook triggers `pupdate run` on directory entry with visible status behavior."
    - "Hook path remains low-noise/non-blocking for prompt responsiveness."
  artifacts:
    - path: "cmd/pupdate/init.go"
      provides: "Shell snippet generation for bash and zsh"
      exports: ["newInitCmd", "resolveShell"]
    - path: "cmd/pupdate/init_test.go"
      provides: "init command contract tests"
    - path: "README.md"
      provides: "Operator instructions for shell setup and status expectations"
  key_links:
    - from: "cmd/pupdate/init.go"
      to: "shell runtime"
      via: "PROMPT_COMMAND/add-zsh-hook snippets"
      pattern: "pupdate run --quiet"
    - from: "README.md"
      to: "user shell config"
      via: "copy-paste init command"
      pattern: "eval \"\$\(pupdate init"
---

<objective>
Finalize shell integration contract and confirm the end-to-end user entrypoint (`init` → hook → run) is clear and verifiable.

Purpose: Convert core run logic into practical day-to-day workflow for bash/zsh users.
Output: Hardened init tests + usage docs + human verification checkpoint.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-mvp-auto-update-cli/01-01-SUMMARY.md
@.planning/phases/01-mvp-auto-update-cli/01-02-SUMMARY.md
@cmd/pupdate/init.go
@cmd/pupdate/run.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock shell snippet contract and test coverage for bash/zsh init</name>
  <files>cmd/pupdate/init.go, cmd/pupdate/init_test.go</files>
  <read_first>cmd/pupdate/init.go, cmd/pupdate/root.go, cmd/pupdate/run.go</read_first>
  <behavior>
    - Test 1: `pupdate init --shell bash` emits snippet containing hook + `pupdate run --quiet`.
    - Test 2: `pupdate init --shell zsh` emits snippet containing `add-zsh-hook` and `pupdate run --quiet`.
    - Test 3: unsupported shell returns non-zero with explicit error text.
  </behavior>
  <action>Create/expand `cmd/pupdate/init_test.go` to enforce snippet contract (per SHELL-01). Keep snippets lightweight (PWD change guard + quiet run) and avoid adding extra shell process calls or filesystem scans (per SHELL-02 performance constraint). Ensure error text remains actionable for unsupported shell values.</action>
  <acceptance_criteria>
    - `cmd/pupdate/init_test.go` exists and includes bash, zsh, and unsupported-shell cases.
    - `cmd/pupdate/init.go` contains `pupdate run --quiet` in both snippets.
    - `go test ./cmd/pupdate -count=1` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd/pupdate -count=1</automated>
  </verify>
  <done>Shell init output contract is protected by tests and remains low-latency friendly.</done>
</task>

<task type="auto">
  <name>Task 2: Add operator docs for init usage and status semantics</name>
  <files>README.md</files>
  <read_first>.planning/ROADMAP.md, cmd/pupdate/init.go, cmd/pupdate/run.go</read_first>
  <action>Create or update `README.md` with concrete sections: `## Quick Start`, `## Shell Hook Setup (bash/zsh)`, `## Run Status Output`, and `## Safety Defaults`. Include copy-paste commands:
`eval "$(pupdate init --shell bash)"` and `eval "$(pupdate init --shell zsh)"`, and document status patterns from plan 02 (`pupdate: skip repo`, `pupdate: skip <ecosystem>`, `pupdate: run`, `pupdate: error`).</action>
  <acceptance_criteria>
    - `README.md` contains `## Shell Hook Setup (bash/zsh)`.
    - `README.md` contains both `eval "$(pupdate init --shell bash)"` and `eval "$(pupdate init --shell zsh)"`.
    - `README.md` contains `pupdate: skip repo`, `pupdate: skip`, `pupdate: run`, and `pupdate: error` strings.
  </acceptance_criteria>
  <verify>
    <automated>go test ./... -count=1</automated>
  </verify>
  <done>Users can install hooks and interpret run statuses without reading source code.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify real interactive shell hook behavior</name>
  <files>cmd/pupdate/init.go, cmd/pupdate/run.go, README.md</files>
  <read_first>cmd/pupdate/init.go, cmd/pupdate/run.go, README.md</read_first>
  <what-built>Tested init snippets, run status messaging, and docs for bash/zsh integration.</what-built>
  <action>Run the interactive shell verification sequence after Task 1-2 automation completes to confirm hook-triggered `pupdate run` behavior in real prompt lifecycle conditions that unit tests cannot fully emulate.</action>
  <how-to-verify>
    1. In an interactive bash shell, run `eval "$(pupdate init --shell bash)"`.
    2. `cd` into a test repo with `bun.lock` or `composer.lock`; confirm status output appears and prompt remains responsive.
    3. Create `.pupignore`; `cd` away and back; confirm skip message appears and no install executes.
    4. Repeat in zsh with `eval "$(pupdate init --shell zsh)"`.
  </how-to-verify>
  <acceptance_criteria>
    - Bash verification run shows status output after directory change without blocking prompt input.
    - Zsh verification run shows status output after directory change without blocking prompt input.
    - `.pupignore` verification shows skip behavior instead of install run in both shells.
  </acceptance_criteria>
  <resume-signal>Type "approved" if behavior matches or describe any mismatch.</resume-signal>
  <verify>
    <automated>MISSING — checkpoint validated by human interactive shell runs after automated tests pass</automated>
  </verify>
  <done>Human confirms hook behavior is correct in real interactive shells.</done>
</task>

</tasks>

<verification>
Run: `go test ./... -count=1` before human checkpoint.
</verification>

<success_criteria>
- `init` emits validated bash/zsh hook snippets.
- README provides exact setup and status guidance.
- Human verification confirms end-to-end shell behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/01-mvp-auto-update-cli/01-03-SUMMARY.md`
</output>
