---
phase: 01-mvp-auto-update-cli
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/freshness/engine.go
  - internal/freshness/engine_test.go
  - internal/state/model.go
  - internal/state/store.go
  - cmd/pupdate/run.go
  - cmd/pupdate/run_test.go
  - cmd/pupdate/run_state_test.go
autonomous: true
requirements:
  - STATE-01
  - STATE-02
  - DET-03
  - SHELL-02
  - STAT-01
must_haves:
  truths:
    - "If lockfiles are unchanged since last success, `pupdate run` skips quickly."
    - "Successful runs persist lockfile hash state in `.pupdate`; failures do not overwrite success state."
    - "A `.pupignore` file causes explicit skip with no install command execution."
    - "Run outcomes are visible as concise skip/run/error statuses."
  artifacts:
    - path: "internal/freshness/engine.go"
      provides: "Hash comparison decisions (update/skip)"
      exports: ["Evaluate"]
    - path: "internal/state/model.go"
      provides: "`.pupdate` schema with per-ecosystem lockfile hashes"
      contains: "type FileState"
    - path: "cmd/pupdate/run.go"
      provides: "CLI execution flow for skip/update/status output"
      exports: ["newRunCmd"]
  key_links:
    - from: "internal/freshness/engine.go"
      to: "cmd/pupdate/run.go"
      via: "DecisionUpdate / DecisionSkip map"
      pattern: "decision\.Decision"
    - from: "cmd/pupdate/run.go"
      to: "internal/state/store.go"
      via: "store.Load / store.Save"
      pattern: "store\.(Load|Save)"
    - from: "cmd/pupdate/run.go"
      to: "repository marker"
      via: "hasPupIgnore('.')"
      pattern: "hasPupIgnore"
---

<objective>
Make skip/state/status behavior explicit, fast, and testable for unchanged dependencies and opt-out repos.

Purpose: Ensure directory-entry invocation stays low-latency and transparent.
Output: Proven skip semantics, stable `.pupdate` persistence, and concise status lines.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@internal/freshness/engine.go
@internal/state/model.go
@internal/state/store.go
@cmd/pupdate/run.go

<interfaces>
From internal/freshness/engine.go:
```go
type EcosystemDecision struct {
    Ecosystem string
    Decision  Decision
    Reason    string
    Lockfiles map[string]string
}
```

From internal/state/model.go:
```go
type FileState struct {
    Version    int
    Ecosystems map[string]EcosystemState
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden lockfile-hash skip semantics and persistence boundaries</name>
  <files>internal/freshness/engine.go, internal/freshness/engine_test.go, internal/state/model.go, internal/state/store.go, cmd/pupdate/run_state_test.go</files>
  <read_first>internal/freshness/engine.go, internal/freshness/engine_test.go, internal/state/model.go, internal/state/store.go, cmd/pupdate/run_state_test.go</read_first>
  <behavior>
    - Test 1: first run (no prior hash) yields update decision.
    - Test 2: unchanged lockfile hashes yield skip decision with explicit reason.
    - Test 3: failed ecosystem outcomes do not mutate prior successful state.
  </behavior>
  <action>Ensure decision and state logic remains strictly lockfile-hash based (per STATE-01, STATE-02, SHELL-02): skip only when stored hashes exactly match, update when missing/changed, and persist `last_success_at` + `lockfiles` only for succeeded ecosystems. Keep `.pupdate` schema at version 1 with deterministic JSON fields and no broad schema expansion in this phase.</action>
  <acceptance_criteria>
    - `internal/freshness/engine.go` retains skip reason string `dependency lockfiles unchanged since last successful run`.
    - `cmd/pupdate/run_state_test.go` contains a test asserting failed ecosystem state is unchanged.
    - `go test ./internal/freshness ./internal/state ./cmd/pupdate -count=1` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/freshness ./internal/state ./cmd/pupdate -count=1</automated>
  </verify>
  <done>Hash-based skip and success-only state persistence are enforced by tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add concise run/skip/error status output for `.pupignore` and freshness outcomes</name>
  <files>cmd/pupdate/run.go, cmd/pupdate/run_test.go</files>
  <read_first>cmd/pupdate/run.go, cmd/pupdate/run_test.go, .planning/ROADMAP.md</read_first>
  <behavior>
    - Test 1: `.pupignore` present prints skip status and executes no installs.
    - Test 2: unchanged ecosystem prints skip status including ecosystem + reason.
    - Test 3: install execution failure prints error status with manager and reason.
  </behavior>
  <action>Refine stderr status lines to concise, grepable forms (per DET-03, STAT-01):
`pupdate: skip repo (.pupignore)`;
`pupdate: skip <ecosystem> (<reason>)`;
`pupdate: run <manager> <args>` before invocation;
`pupdate: error <manager> install failed: <err>` on failure.
Do not remove existing machine-readable JSON payload on stdout; status lines belong on stderr for shell-hook visibility.</action>
  <acceptance_criteria>
    - `cmd/pupdate/run.go` contains literal prefixes `pupdate: skip repo`, `pupdate: skip `, `pupdate: run `, and `pupdate: error `.
    - `cmd/pupdate/run_test.go` asserts `.pupignore` skip messaging.
    - `go test ./cmd/pupdate -count=1` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd/pupdate -count=1</automated>
  </verify>
  <done>Users get concise status feedback for skip/run/error while behavior stays non-blocking.</done>
</task>

</tasks>

<verification>
Run: `go test ./cmd/pupdate ./internal/freshness ./internal/state -count=1`
</verification>

<success_criteria>
- `.pupignore` causes explicit skip with no install attempts.
- Unchanged lockfiles skip quickly and clearly.
- `.pupdate` persists only successful ecosystem updates.
</success_criteria>

<output>
After completion, create `.planning/phases/01-mvp-auto-update-cli/01-02-SUMMARY.md`
</output>
