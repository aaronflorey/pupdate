---
phase: 11-kasetto-and-freshness-correctness-follow-ups
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: []
---

# Plan 11-01 - Make Kasetto execution project-scoped

## Objective

Make Kasetto installs execute against the detected project scope so local project detection cannot mutate or depend on ambient global Kasetto state.

## Scope

- Audit the Kasetto install path in `cmd/pupdate/run_install.go` and any detection-result fields available during install planning.
- Update the Kasetto manager plan so execution is anchored to the detected project instead of generic `kst sync` resolution.
- Add or adjust `cmd/pupdate` regression coverage proving project-scoped execution arguments are emitted for Kasetto runs.

## Tasks

1. Confirm what project-scoping input is available from Kasetto detection results today and what minimal additional wiring is needed in install planning.
2. Implement the narrowest change that makes Kasetto execution explicitly project-scoped.
3. Re-run focused command-package tests covering emitted run lines and execution arguments.

## Verification

- `go test ./cmd/pupdate -count=1`

## Done When

- Kasetto execution no longer relies on ambient/global tool resolution alone after local project detection succeeds.
- Regression tests encode the intended project-scoped behavior.
