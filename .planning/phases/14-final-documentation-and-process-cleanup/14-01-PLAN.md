---
phase: 14-final-documentation-and-process-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: []
---

# Plan 14-01 - Update README config behavior docs

## Objective

Update README config behavior documentation so it matches the current missing-config semantics: default-effective behavior without automatic config-file creation.

## Scope

- Audit the `pupdate run` and `pupdate config` README sections for wording that still implies config auto-creation.
- Update the README to describe the current non-auto-creating behavior introduced by Phase 09 plan 06.
- Keep the change limited to the incorrect config-behavior documentation unless nearby wording must move for clarity.

## Tasks

1. Confirm the current intended missing-config behavior from the Phase 09 artifacts and current README text.
2. Update the narrowest README lines that still claim missing config creates directories or `config.yaml` automatically.
3. Re-read the surrounding README config section for consistency after the wording change.

## Verification

- Manual review that README config behavior matches the current missing-config defaults contract.

## Done When

- README no longer claims that `pupdate run` or `pupdate config` auto-creates config files when they are missing.
- The documented config behavior matches the current implicit-defaults model.
