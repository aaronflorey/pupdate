---
phase: 03-v1-release-automation-and-milestone-closeout
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [REL-01, REL-02, REL-03]
---

# Plan 03-01 - Release automation wiring

## Objective

Implement GitHub-native semver release automation so tagged releases build and publish pupdate binaries through GoReleaser.

## Scope

- Add Release Please configuration files.
- Add GitHub workflow(s) for CI checks and release orchestration.
- Ensure workflows call existing `.goreleaser.yaml` with `v*` tag semantics.

## Tasks

1. Add Release Please manifest/config and set package component for `pupdate`.
2. Add/update GitHub Actions workflow for push/PR test coverage (`go test ./...`).
3. Add release workflow that runs on release tags and executes GoReleaser.
4. Document release flow entry points in `README.md` or release docs.

## Verification

- `go test ./... -count=1`
- Validate workflow YAML syntax and trigger scopes.

## Done When

- Repository includes Release Please + workflow config.
- Release path for semver tags is explicit and reproducible.
