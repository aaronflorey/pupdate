# P3-T6: Document folder blacklist behavior

Status: pending
Type: implementation
Phase: 3 monorepo scan expansion
Depends on: P3-T4, P3-T5
PRD refs: inserted user idea on 2026-06-04: "allow blacklisting folders so we don't enter them. eg. `blah` so ./blah/ or ./foo/blah would be blacklisted."
Approved-plan refs: `.planning/quick/phase-15-performance-diagnostics-config-hook-planning.md`

## Objective

Document the folder blacklist behavior, exact-match semantics, and examples in the README.

## Context

- The new exclusion behavior is user-configured and needs concrete examples so users understand that matches are exact directory names.
- The docs must explain the interaction with the existing shallow default scan and the planned `workspace_globs` feature without implying a broader pattern language.
- README wording should make clear that blacklist entries suppress directory traversal rather than disabling the entire repo like `.pupignore`.

## Expected files

Start with these expected files. You may create or modify another file only when it is directly required for this TODO, consistent with the approved plan, and documented in your report:

- `README.md`: document the folder blacklist setting, semantics, and examples.

## Required changes

1. Add the folder blacklist setting to the config documentation.
2. Show examples demonstrating that `blah` excludes `./blah` and `./foo/blah` via exact directory-name matching.
3. Explain how the blacklist differs from `.pupignore` and how it interacts with the default shallow scan and configured workspace expansion.

## Non-goals

- Do not change runtime behavior or config parsing in this task.
- Do not broaden the docs into unrelated installation or platform support topics.
- Do not document unimplemented glob or path-pattern blacklist behavior.

## Acceptance criteria

- README documents the new setting and its exact-match directory-name semantics.
- README examples demonstrate the intended `blah` behavior concretely.
- README distinguishes folder blacklist traversal skips from repo-wide `.pupignore` behavior.

## Checks

Run or inspect:

- Manual inspection of README config docs and examples

## Report requirements

The executor must report changed files, any files changed outside the expected list with rationale, checks run, acceptance criteria status, assumptions, and blockers.

## Blocker policy

Do not stop just because the expected file list is incomplete, a directly required adjacent file must change, a helper/test/seam is missing, checks fail, or an existing generated artifact should be reused. Make the smallest plan-consistent change and report the rationale.

Stop only if the task needs an unapproved product, public API or CLI, persistence or migration, security, dependency or tooling, architecture, external integration, or UX decision.

## Generated artifacts

If a generated artifact or generated equivalent already exists, reuse it or adapt references to it instead of creating a duplicate. Stop only if choosing or changing the artifact would affect a real blocker area.

## Review instructions

The reviewer must verify:

- The implementation matches this brief and does not add unrequested behavior.
- All acceptance criteria are satisfied.
- Required tests or checks pass, or failures are explained and unrelated.
- No later-phase work was pulled into this task.
- Any file changed outside the expected list was directly required, plan-consistent, and explained.

## Stop conditions

Stop and ask the orchestrator or user if:

- The implementation does not settle the exact user-facing config naming cleanly.
- Documenting the feature would require describing behavior that is not yet implemented or tested.
- The work would require editing files outside the allowed list.
