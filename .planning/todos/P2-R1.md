# P2-R1: Review shared preflight and status guidance

Status: complete
Type: review
Phase: 2 shared preflight flow
Depends on: P2-T1, P2-T2
PRD refs: `.planning/update.md` -> `Maintainability` / `6`; `UX and Adoption` / `8`
Approved-plan refs: `.planning/quick/phase-15-performance-diagnostics-config-hook-planning.md`

## Objective

Review the shared preflight and status-guidance phase for behavior drift, scope creep, and missing regression coverage.

## Context

- This phase refactors hot-path command setup and changes user-visible status text.
- Small behavior drift between `run` and `status` is a core risk of the refactor.
- The review must verify that the new guidance explains existing states rather than inventing new decision branches.

## Expected files

Create or modify only these files unless a stop condition applies:

- `.planning/TODO.md`: update statuses if the phase is accepted.
- `.planning/TODO-TRACE.md`: add any repair mapping if the review finds a gap.
- `.planning/todos/P2-R1.md`: append reviewer notes if needed.

## Required changes

1. Compare all phase-2 implementation work against P2-T1 and P2-T2.
2. Re-check the PRD requirements for shared preflight logic and action-oriented status guidance.
3. Accept the phase or create focused repair tasks if behavior drift or missing coverage is found.

## Non-goals

- Do not implement new feature scope in this review task.
- Do not start monorepo config or install-docs work here.

## Acceptance criteria

- The reviewer compared code against all implementation task briefs in the phase.
- The reviewer checked relevant PRD and approved-plan requirements.
- The reviewer identified scope drift, missing tests, regressions, and incomplete acceptance criteria.
- The reviewer either accepted the phase or created/updated focused repair TODOs.

## Checks

Run or inspect:

- Review `cmd/pupdate` diffs for shared-preflight ownership and `status` guidance scope
- `go test ./cmd/pupdate -count=1`

## Review instructions

The reviewer must verify:

- The implementation matches this brief and does not add unrequested behavior.
- All acceptance criteria are satisfied.
- Required tests or checks pass, or failures are explained and unrelated.
- No later-phase work was pulled into this task.

## Stop conditions

Stop and ask the orchestrator or user if:

- The phase requires a new CLI contract that is not already approved.
- Repair work would require changes outside the phase-2 file lists.
- The refactor changes hot-path behavior in ways the current tests do not explain.

## Reviewer notes

- 2026-06-04 review: accepted phase 2 as delivered.
- `cmd/pupdate/preflight.go` now owns the shared config/root/ignore/state/detection/freshness collection path, while `cmd/pupdate/run_execution.go` and `cmd/pupdate/status.go` keep their distinct skip and output behavior by passing different preflight options.
- `cmd/pupdate/run_test.go` and `cmd/pupdate/status_test.go` cover the shared preflight skip cases (`$HOME`, configured roots, `.pupignore`), state-warning behavior, and the added action-oriented status guidance for active hook locks and missing managers on `PATH`.
- `mise exec -- go test ./cmd/pupdate -count=1` passed during this review.
- No focused repair TODOs were required from this pass.
