# P3-R1: Review monorepo scan expansion

Status: pending
Type: review
Phase: 3 monorepo scan expansion
Depends on: P3-T1, P3-T4, P3-T2, P3-T5, P3-T3, P3-T6
PRD refs: `.planning/update.md` -> `Product Gaps` / `2`; inserted user idea on 2026-06-04: "allow blacklisting folders so we don't enter them. eg. `blah` so ./blah/ or ./foo/blah would be blacklisted."
Approved-plan refs: `.planning/quick/phase-15-performance-diagnostics-config-hook-planning.md`

## Objective

Review the monorepo-detection phase against the latency, exclusion, and low-surprise constraints.

## Context

- This phase adds new config surfaces for opt-in workspace expansion and exact-name folder blacklist behavior.
- The main risks are default-path regressions, ambiguous matching semantics, and docs drifting from implementation.
- Review must confirm the phase preserved the low-latency default contract outside configured blacklist exclusions and opt-in workspace expansions.

## Expected files

Create or modify only these files unless a stop condition applies:

- `.planning/TODO.md`: update statuses if the phase is accepted.
- `.planning/TODO-TRACE.md`: add any repair mapping if the review finds a gap.
- `.planning/todos/P3-R1.md`: append reviewer notes if needed.

## Required changes

1. Compare the config, detection, and README work against P3-T1 through P3-T6.
2. Confirm `workspace_globs` remains opt-in and folder blacklist entries apply as exact directory-name matches.
3. Confirm the default scan path stays unchanged except for explicitly blacklisted directories.
4. Accept the phase or create focused repair tasks for any latency, correctness, or documentation mismatch.

## Non-goals

- Do not implement new detection behavior in this review task.
- Do not start install-docs or integration-test work here.

## Acceptance criteria

- The reviewer compared code against all implementation task briefs in the phase.
- The reviewer checked relevant PRD, inserted-idea, and approved-plan requirements.
- The reviewer identified scope drift, missing tests, regressions, and incomplete acceptance criteria.
- The reviewer either accepted the phase or created/updated focused repair TODOs.

## Checks

Run or inspect:

- Review detection and config diffs
- `go test ./cmd/pupdate ./internal/detection -count=1`
- `go test ./internal/detection -run '^$' -bench 'BenchmarkDetectProjectTree' -count=1`
- Manual inspection of README config examples for `workspace_globs`, folder blacklist, and `.pupignore` distinctions

## Review instructions

The reviewer must verify:

- The implementation matches this brief and does not add unrequested behavior.
- All acceptance criteria are satisfied.
- Required tests or checks pass, or failures are explained and unrelated.
- No later-phase work was pulled into this task.

## Stop conditions

Stop and ask the orchestrator or user if:

- Benchmark or test results suggest the default path regressed materially.
- Folder blacklist matching semantics appear to require a broader model than exact directory-name entries.
- Repair work would require changing files outside the phase-3 task lists.
