# P3-T3: Document workspace_globs behavior

Status: complete
Type: implementation
Phase: 3 monorepo scan expansion
Depends on: P3-T2
PRD refs: `.planning/update.md` -> `Product Gaps` / `2. Revisit detection depth for common monorepo layouts`
Approved-plan refs: `.planning/quick/phase-15-performance-diagnostics-config-hook-planning.md`

## Objective

Document `workspace_globs` behavior, defaults, and examples in the README.

## Context

- The new scan-expansion behavior is opt-in and must stay easy to understand.
- Users need examples for common monorepo layouts without being told that broader scanning is automatic.
- The docs must stay aligned with the shallow default performance contract.

## Expected files

Create or modify only these files unless a stop condition applies:

- `README.md`: document `workspace_globs` config behavior, examples, and the unchanged default scan.

## Required changes

1. Add `workspace_globs` to the config documentation.
2. Show one or more example values for common layouts such as `apps/*` or `services/*`.
3. State clearly that the default scan remains shallow unless users opt in.

## Non-goals

- Do not change runtime behavior or config parsing in this task.
- Do not broaden the docs into unrelated installation or platform support topics.

## Acceptance criteria

- README documents the new setting and its default behavior.
- README examples demonstrate opt-in monorepo expansion concretely.
- README still describes the shallow default traversal accurately.

## Checks

Run or inspect:

- Manual inspection of README config and behavior sections

## Review instructions

The reviewer must verify:

- The implementation matches this brief and does not add unrequested behavior.
- All acceptance criteria are satisfied.
- Required tests or checks pass, or failures are explained and unrelated.
- No later-phase work was pulled into this task.

## Stop conditions

Stop and ask the orchestrator or user if:

- The implementation does not settle the exact user-facing setting shape cleanly.
- Documenting the feature would require describing behavior that is not yet implemented or tested.
- The work would require editing files outside the allowed list.
